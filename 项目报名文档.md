# MonadLens 项目报名文档

> LXDAO × Monad 线上黑客松参赛作品
> 提交前请确认：演示地址已填入并可公网访问、GitHub 仓库已设为公开。

---

## 一、项目定位

MonadLens 是面向 Monad 生态的**对话式链上助手 + 签名前后果透镜**。

用户用中文描述想做的事，AI Agent 负责构造交易；但在用户签名之前，系统通过 Moss SDK 在真实链上状态中**模拟执行**这笔交易，把"转出什么、收到什么、授权给谁、钱最终落到哪个地址"用人话摊开，并对收款方掉包、金额膨胀、无限授权等攻击实时拦截。

一句话定位：**让用户在签字之前，真正看懂这笔交易会带来的后果。**

---

## 二、主要亮点

1. **签名前后果透镜（核心能力）**
   基于 Moss 的 `debug_traceCall` 在真实链上状态模拟，把资金流出 / 流入、授权明细、收款方对账、安全告警逐项展示，签名前即知后果。

2. **应用层「收款方对账」（差异化能力）**
   Moss 的信封约束只保证"转出多少"，不约束"转给谁"。MonadLens 补上这一层：当界面告诉用户的收款方，与实际 calldata 的收款方不一致时，产出 `UNDECLARED_RECIPIENT` 告警并拦截——抓住"金额一分不差、收款地址被换"的隐蔽攻击。

3. **五类攻击护栏，全部拦截**
   金额膨胀、无限授权、夹带授权、收款方掉包、封印后篡改。任一命中即判定 `blocked`，**签名按钮直接锁死**（不是提示，是拦住）。

4. **Agent 双脑设计，演示永不翻车**
   规则引擎（纯正则、零依赖、零幻觉）作为兜底，LLM（DeepSeek 等 OpenAI 兼容接口）作为增强；LLM 任何失败自动回落规则引擎并标注"降级"。

5. **实时链上仪表盘**
   WebSocket 订阅 Monad 区块，实时展示 TPS、出块间隔、Gas 占用、基础费与活跃地址 / 合约榜，让"网络现在怎么样"一眼可见。

6. **钱包与新手友好**
   一键把 Monad 网络添加到钱包；主网余额预检；发送失败的错误翻译成中文提示（不再抛出原始技术堆栈）。

7. **三栏一屏闭环**
   左栏看网络、中栏说需求、右栏看后果，一屏内完成"看网络 → 说需求 → 看后果 → 签字"。

---

## 三、使用场景

- **普通用户防骗**：在签名前核对 AI Agent 构造的交易，避免"Agent 说的"和"calldata 真正做的"不是一回事。
- **安全演示 / 审计**：一键注入攻击，直观展示"为什么这笔交易不能签"，适合现场讲解与教学。
- **Agent 开发者**：把 Moss 模拟接入自己的 Agent 工作流，作为签名前的强制闸门。
- **零成本体验**：切换测试网 + 免费水龙头，无需真实资产即可跑通"模拟 → 签名 → 上链 → 浏览器验证"完整闭环。

---

## 四、技术信息

- **演示地址**：`https://<部署后获得，例如 monadlens.vercel.app>`（需部署到公网后填入，详见第五节）
- **GitHub 仓库**：`https://github.com/<你的用户名>/monadlens`（提交时保持公开）
- **技术栈**：Next.js 16（App Router / Turbopack）· React 19 · TypeScript · Tailwind v4 · wagmi v3 + viem v2 · zustand · recharts · @themoss/core + @themoss/simulator
- **本地运行**：
  ```bash
  npm install
  npm run dev        # http://localhost:3000
  ```
- **支持网络**：Monad 主网（chainId 143）/ 测试网（chainId 10143），通过 `NEXT_PUBLIC_MONAD_NETWORK` 一键切换。
- **许可证**：MIT

---

## 五、如何获得"可直接访问的演示地址"

本项目为 Next.js 全栈应用（含服务端模拟接口），本地 `npm run dev` 仅在本地可访问，需部署到支持 Node.js 的运行环境。推荐 Vercel：

1. 把仓库推到 GitHub（保持公开）。
2. 在 Vercel 导入该仓库，`Build Command` 用 `npm run build`，无需额外配置。
3. 部署完成后获得形如 `https://monadlens.vercel.app` 的公开地址，填入上方「演示地址」即可。

> 说明：演示接口依赖 `debug_traceCall`，运行时必须为 Node.js（已在 `next.config.ts` 配置 `runtime = "nodejs"`）。

---

## 六、报名简介（可直接复制粘贴，约 220 字）

MonadLens 是 Monad 生态的对话式链上助手与签名前后果透镜。用户用中文描述意图，AI Agent 构造交易；在签名之前，系统通过 Moss SDK 在真实链上状态中模拟执行，把资金流向、授权对象与收款方对账用人话摊开，并对金额膨胀、无限授权、收款方掉包等五类攻击实时拦截（命中即锁死签名按钮）。其差异化能力是在应用层补上 Moss 未覆盖的"收款方对账"，可识别金额不变但收款地址被替换的隐蔽攻击。项目含实时链上仪表盘、Agent 双脑（规则引擎 + LLM 自动降级）与一键添加 Monad 网络等特性，支持主网 / 测试网一键切换，适用于用户防骗、安全演示与 Agent 开发者接入。
