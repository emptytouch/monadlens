# MonadLens 环境变量示例
# 复制为 .env.local 后按需填写（.env.local 不被 git 追踪）

# ---------------------------------------------------------------------------
# RPC 端点（可选）
# 默认只用公共节点 https://rpc.monad.xyz，但该节点偶尔慢/不稳。
# 可在此追加备用或私人节点（逗号分隔，第一个是主用），viem 的 fallback
# 传输层会在主节点失败时自动切换。
# 注意：NEXT_PUBLIC_ 前缀的变量会打进浏览器 bundle，必须可公开。
# ---------------------------------------------------------------------------
# NEXT_PUBLIC_MONAD_RPC_URLS=https://rpc.monad.xyz,https://monad.drpc.org
# NEXT_PUBLIC_MONAD_RPC_WS_URLS=wss://rpc.monad.xyz

# ---------------------------------------------------------------------------
# 网络切换（可选，默认 mainnet）
#   mainnet = 主网 chainId 143，真实 MON，无水龙头
#   testnet = 测试网 chainId 10143，免费水龙头 https://faucet.monad.xyz
# 设为 testnet 后可在没有 MON 的情况下测试真实签名广播；正式演示前改回 mainnet。
# NEXT_PUBLIC_ 变量改后必须重新 `next build` 才生效。
# ---------------------------------------------------------------------------
# NEXT_PUBLIC_MONAD_NETWORK=mainnet
# NEXT_PUBLIC_MONAD_TESTNET_RPC_URLS=https://testnet-rpc.monad.xyz,https://rpc-testnet.monadinfra.com
# NEXT_PUBLIC_MONAD_TESTNET_RPC_WS_URLS=wss://testnet-rpc.monad.xyz,wss://rpc-testnet.monadinfra.com

# ---------------------------------------------------------------------------
# LLM 双脑（可选）
# 不配置时 Agent 走内置规则引擎（零依赖、零幻觉，演示足够）。
# 配置后启用 LLM 增强（OpenAI 兼容接口，支持 DeepSeek / Moonshot / Zhipu 等）。
# 以下为服务端变量，无需 NEXT_PUBLIC_ 前缀，不会进浏览器。
# ---------------------------------------------------------------------------
# LLM_API_KEY=sk-xxxxxxxx
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_MODEL=gpt-4o-mini
